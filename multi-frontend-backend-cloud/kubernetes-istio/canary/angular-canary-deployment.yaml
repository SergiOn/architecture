apiVersion: apps/v1
kind: Deployment
metadata:
  name: angular-canary-deployment
  labels:
    app: angular-app
    release: canary
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: angular-app
      release: canary
  template:
    metadata:
      name: angular-canary
      labels:
        app: angular-app
        release: canary
    spec:
      containers:
        - name: angular-canary-container
          image: serhiion/angular:2019-08-11
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
#          readinessProbe:
#            httpGet:
#              path: /
#              port: 80
#            initialDelaySeconds: 1
#            timeoutSeconds: 1
#          livenessProbe:
#            httpGet:
#              path: /
#              port: 80
#            initialDelaySeconds: 1
#            timeoutSeconds: 1
      restartPolicy: Always
